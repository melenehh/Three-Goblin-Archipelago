enum class EOptimizationBaseType {
    PlayerPawn = 0,
    PlayerCamera = 1,
    EOptimizationBaseType_MAX = 2,
};

enum class EOptimizationWave {
    Invisible = 0,
    NoOptimization = 1,
    FirstWave = 2,
    SecondWave = 3,
    ThirdWave = 4,
    EOptimizationWave_MAX = 5,
};

